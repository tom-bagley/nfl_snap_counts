{
  "name": "nfl-snap-counts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:nfl-data": "node scripts/prepare-data.mjs",
    "prepare:college-data": "node scripts/prepare-college-data.mjs",
    "prepare-data": "npm run prepare:nfl-data && npm run prepare:college-data",
    "collect:snap-counts": "node scripts/collect-snap-counts.mjs",
    "collect:depth-charts": "node scripts/refresh-depth-charts.mjs",
    "collect:college-data": "node scripts/collect-college-data.mjs",
    "collect:data": "node scripts/collect-data.mjs",
    "dev": "npm run prepare-data && vite",
    "build": "npm run prepare-data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.2",
    "cheerio": "^1.2.0",
    "vite": "^7.1.3"
  }
}
